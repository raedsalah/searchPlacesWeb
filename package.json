{
  "name": "searchPlacesWeb",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace server dev\" \"yarn workspace web dev\"",
    "build": "concurrently \"yarn workspace server build\" \"yarn workspace web build\"",
    "start": "concurrently \"yarn workspace server start\" \"yarn workspace web start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
